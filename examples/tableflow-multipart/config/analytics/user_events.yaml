tables:
  - source_cluster_name: kafka_cluster_1
    source_topic: user_events
    source_format: json
    schema_mode: inline
    input_schema: |
      {
        "type": "object",
        "properties": {
          "user_id": { "type": "string" },
          "action": { "type": "string" },
          "timestamp": { "type": "long" }
        },
        "required": ["user_id", "action", "timestamp"]
      }
